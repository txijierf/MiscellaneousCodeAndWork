#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>

unsigned int fibonacciSeq(unsigned int fib)

int main(void)
{
unsigned int f, retrnInt;
printf("Enter how many numbers to be added in the sequence: ");
scanf("%d", &f);

retrnInt = fibonacciSeq(f);
printf("The result of the fibonacci sequence is: %d", retrnInt);
}

unsigned int fibonacciSeq(unsigned int fib)
{
int i = 0;
for(i = fib; i>=0; i--)
	{
		
	}
}

